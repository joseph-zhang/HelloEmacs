;; in previous code, all the configuration are written in one file
;; we know that configuration is always complex,
;; use modularity can make the configuration clear.

;; for example, in a common configuration ~/.emacs.d/init.el file
;; there are many different code used for different way, some for UI, some for keybinding ..
;; it is better to write then separately in different .el files.
